const car = {
    make: 'Honda',
    model: 'Accord',
    year: 2020
}

// this is to get the make: model: and also year not the result , thats what object does 
const keys = Object.keys(car)

console.log(keys);

